> = overwrite function
>> = add to whatever is there function
< = same as > but opposite direction
| = used for stringing together commands
grep = search function 
sort = alphabetical order
sed = find/replace         syntax ="sed 's/snow/rain/g'"
uniq = deleted identicle, side by side, items 
export = creates a variable, and stores it in the enviornment
env = shows a list of all enviornment variables


$PATH = unix operating system needs a filepath*commands are all stored somewhere
$HOME = runnig 
$USER = echoing this is who am I command
$PS1 = main prompt


